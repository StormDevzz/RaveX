package volthack.modules.player

import volthack.setting.Category
import volthack.setting.Module

object MultiTask : Module("MultiTask", "Allows you to eat, use items, draw bows, mine blocks, and attack entities simultaneously", Category.PLAYER) {
    // MultiTask is a toggle module whose action is managed directly via LocalPlayer mixin.
}
